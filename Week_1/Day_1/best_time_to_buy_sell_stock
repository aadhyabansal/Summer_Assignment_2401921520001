class Solution {
    public int maxProfit(int[] prices) {
        int ans=0, n=prices.length, sell=prices[n-1];
        for(int i=n-2; i>=0; i--){
            ans=Math.max(sell-prices[i],ans);
            sell=Math.max(sell,prices[i]);
        }
        return ans;
    }
}